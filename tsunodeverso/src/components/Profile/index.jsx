import { Container } from './styles';

import profile from '../../assets/profile.webp';

export const Profile = ({}) => {
    return (
        <Container>
            <img src={profile} alt='' />
        </Container>
    )
}